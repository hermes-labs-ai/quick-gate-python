[tool.ruff]
target-version = "py312"
exclude = [".pygate", "action-source"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
include = ["check.py"]
exclude = [".pygate", "action-source"]
