[tool.ruff]
target-version = "py312"
exclude = [".pygate", "action-source"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
include = ["check.py"]
exclude = [".pygate", "action-source"]
