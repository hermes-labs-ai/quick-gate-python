[tool.ruff]
target-version = "py312"
exclude = [".pygate", "action-source"]

[tool.ruff.lint]
ignore = ["RUF022"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
include = ["check.py"]
exclude = [".pygate", "action-source"]

[tool.pytest.ini_options]
testpaths = ["test_full.py"]
